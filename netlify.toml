[build]
  command = "npm run build"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "22"  # specify your Node.js version

# For single page applications (if applicable)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200