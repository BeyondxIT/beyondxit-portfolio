[build]
  command = "npm run build"
  publish = "dist"  # or your build output directory

[build.environment]
  NODE_VERSION = "18"  # specify your Node.js version

# For single page applications (if applicable)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200